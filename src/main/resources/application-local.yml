
server:
  port: 8443
   #ssl:
#    key-store-type: pkcs12
#    key-alias: local
#    enabled: true
#    key-store:
#    key-store-password:

logging:
  level:
    org.hibernate.type: trace

spring:
  jpa:
    hibernate:
      ddl-auto: update
